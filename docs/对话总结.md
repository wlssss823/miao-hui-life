# 对话总结

## 已生成的文档

| 文件 | 内容 |
|------|------|
| `MQ.md` | RabbitMQ 使用说明（秒杀异步削峰） |
| `Redis.md` | Redis 使用说明（全部业务场景 + Lua + Redisson） |
| `秒杀逻辑.md` | 秒杀完整数据流（三层幂等、六步校验、MQ 削峰） |
| `亮点.md` | 项目十大亮点总结 |

均已存入飞书知识库"示例知识库 / Wiki samples"。

## 飞书 CLI 集成

- 安装了 `@larksuite/cli` v1.0.36 + 19 个 Skills
- 创建飞书应用（App ID: `cli_aa845f318af99cc6`）
- 完成 Bot 身份 + 用户身份授权
- 建立了 P2P 私聊（chat_id: `oc_903a0d1043b3416bee19379d1b455146`）和群聊"Claude Code 测试"

## MCP Server（实时消息）

为实现在飞书发消息后自动回复，在 `D:\Git_repo\xmdp-feishu-mcp\` 创建了 MCP 服务器：

- **技术栈**: Node.js + `@modelcontextprotocol/sdk`
- **轮询机制**: 每 5 秒检查飞书 P2P 私聊新消息
- **暴露工具**:
  - `feishu_check` — 获取未读消息
  - `feishu_send` — 发送文本
  - `feishu_send_markdown` — 发送 Markdown
- **配置**: `.mcp.json` + `settings.local.json` 已就绪，重启 Claude Code 后生效

## 尝试过但已停用的方案

- **定时间轮询**: 用 CronCreate 每 3 分钟检查飞书，已停用
